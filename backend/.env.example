# HTTP port for the Nest server (default: 5174)
PORT=5174

# Number of parallel Whisper worker processes.
# Defaults to min(4, cpuCount - 1) when unset.
WORKERS=

# Absolute path to an ffmpeg binary. Leave blank to auto-detect
# (falls back to ffmpeg-static, then the system PATH).
FFMPEG_PATH=
